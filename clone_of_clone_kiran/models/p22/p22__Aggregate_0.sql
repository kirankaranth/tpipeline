{{
  config({    
    "materialized": "ephemeral",
    "database": "hive_metastore",
    "schema": "prophecy_rajat_dev_cloud2345"
  })
}}

WITH Aggregate_0 AS (

  SELECT * 
  
  FROM `` AS in0

)

SELECT *

FROM Aggregate_0
